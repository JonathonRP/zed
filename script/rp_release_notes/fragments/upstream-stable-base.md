## Official stable foundation

This RP release is rebuilt on the pinned official Zed stable tag recorded in
`.github/rp-stable-base.json`, rather than unreleased upstream development code.
The release manifest records the upstream tag, its immutable commit, the Zed
semantic version, and the RP commit as one compatibility identity.
