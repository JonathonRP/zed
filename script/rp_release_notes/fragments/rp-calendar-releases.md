## RP release experience

- Adds independent UTC calendar release identities, offline release notes, and a durable once-per-release notes tab without changing Zed's internal version or commit identity.
- Fixes the Settings UI renderer for `lsp_document_color_inlay_shape`, so the Square/Circle dropdown appears instead of `NO RENDERER`.
